# LabNotebook

该项目为一个简易的实验记录本，使用 Python 编写，适合在无外网环境下本地部署。

## 功能
- 图形界面基于 `Tkinter`，可进行项目和实验的增删改查；
- 支持上传文本或 Excel 等数据文件，并自动解析；
- 记录数据与实验的关联，可查看每个实验上传的文件；
- 提供简单的项目汇总，统计实验数量和数据文件数量；
- 所有数据存储在本地 `SQLite` 数据库 `labnotebook.db`。

## 使用
运行 `python labnotebook.py` 启动界面即可。
